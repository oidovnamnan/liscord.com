# 🖨️ LISCORD — PDF / ХЭВЛЭХ МОДУЛЬ

> PDF үүсгэх, баримт хэвлэх, thermal printer дэмжлэг.

---

## 1. PDF TEMPLATE ЖАГСААЛТ

| # | Template ID | Нэр | Хэзээ | Хэмжээ |
|---|-------------|------|-------|--------|
| 1 | `invoice` | Нэхэмжлэл | Захиалгад нэхэмжлэл илгээх | A4 |
| 2 | `receipt` | Баримт (хураангуй) | Төлбөр бүртгэхэд | A5 / Thermal |
| 3 | `order_summary` | Захиалгын хуудас | Захиалга хэвлэх | A4 |
| 4 | `delivery_note` | Хүргэлтийн хуудас | Хүргэгчид өгөх | A5 |
| 5 | `packing_slip` | Боодлын хуудас | Агуулахад | A5 |
| 6 | `payslip` | Цалингийн хуудас | Сар бүр ажилтанд | A4 |
| 7 | `repair_ticket` | Засварын тасалбар | Засварт хүлээн авахад | A5 / Thermal |
| 8 | `customer_statement` | Тооцооны хуудас | Харилцагчийн тооцоо | A4 |
| 9 | `purchase_order` | PO хуудас | Нийлүүлэгчид илгээх | A4 |
| 10 | `kitchen_ticket` | Гал тогооны тасалбар | Хоол бизнест | Thermal 80mm |
| 11 | `report_export` | Тайлангийн PDF | Тайлан татахад | A4 |
| 12 | `warranty_card` | Баталгааны карт | Засвар бизнест | A5 |

---

## 2. PDF TEMPLATE БҮТЭЦ

```javascript
// Template-ийн ерөнхий бүтэц
{
  // Header
  businessLogo: "url",
  businessName: "Эрээн Карго",
  businessAddress: "УБ, БЗД",
  businessPhone: "+976 7700 1234",
  businessEmail: "info@cargo.mn",
  taxId: "НӨАТ: 1234567",
  
  // Body — template бүрт өөр
  // ...
  
  // Footer
  notes: "Баярлалаа!",
  terms: "Бараа хүлээн авснаас 3 хоногийн дотор буцаалт хийж болно.",
  qrCode: "https://liscord.com/track/xxx",   // QR код
  barcode: "ORD-0042",                        // Баркод
  generatedAt: "2026.02.22 15:30",
  poweredBy: "Powered by Liscord",
}
```

---

## 3. НЭХЭМЖЛЭЛ (Invoice) ЗАГВАР

```
┌────────────────────────────────────────┐
│ [ЛОГО]  Эрээн Карго ХХК               │
│         УБ, БЗД, 3-р хороо            │
│         +976 7700 1234                  │
│         НӨАТ: 1234567                   │
│                                        │
│  НЭХЭМЖЛЭЛ #INV-0042                  │
│  Огноо: 2026.02.22                     │
│  Төлбөрийн хугацаа: 2026.03.22        │
├────────────────────────────────────────┤
│ ХАРИЛЦАГЧ:                             │
│ Болд • +976 8800 1234                  │
│ БЗД, 3-р хороо, 45-р байр             │
├────────────────────────────────────────┤
│ #  Бараа            Тоо   Үнэ    Дүн  │
│ 1  iPhone 15 Pro     2   4.5M   9.0M  │
│ 2  Case              2   15K    30K   │
├────────────────────────────────────────┤
│                   Дүн:     ₮9,030,000  │
│                   Хөнгөлөлт:-₮0       │
│                   Хүргэлт: +₮5,000    │
│                   НӨАТ(10%):₮821,364   │
│                   НИЙТ:   ₮9,035,000  │
│                                        │
│ Төлсөн:   ₮5,000,000                  │
│ Үлдэгдэл: ₮4,035,000                  │
├────────────────────────────────────────┤
│ Данс: Хаан банк 5000 1234 5678        │
│       Эрээн Карго ХХК                  │
│                                        │
│ [QR КОД]   Powered by Liscord         │
└────────────────────────────────────────┘
```

---

## 4. THERMAL PRINTER (POS)

### 4.1 Thermal баримт (80mm)
```
================================
     Эрээн Карго
     +976 7700 1234
================================
Баримт: #ORD-0042
Огноо:  2026.02.22 15:30
Ажилтан: Бат
--------------------------------
Бараа              Тоо    Дүн
iPhone 15 Pro       2   9,000,000
Case                2      30,000
--------------------------------
Дүн:                    9,030,000
Хүргэлт:                   5,000
НИЙТ:                   9,035,000
Төлсөн:                5,000,000
Үлдэгдэл:              4,035,000
--------------------------------
Төлбөр: Банк шилжүүлэг
Данс: Хаан банк ****5678
================================
    Баярлалаа! ♥
================================
```

### 4.2 Thermal дэмжлэг
- Web Bluetooth API (Bluetooth thermal printer)
- USB printer → Browser Print API
- Гал тогооны тасалбар (хоол бизнест) → автомат принт
- Silent print (popup-гүй шууд хэвлэх)

---

## 5. ХЭРЭГЖҮҮЛЭЛТ

### 5.1 Client-side PDF (jsPDF + html2canvas)
```javascript
// Жижиг PDF-д — client-side
import jsPDF from 'jspdf';
import html2canvas from 'html2canvas';

async function generatePDF(elementId, filename) {
  const element = document.getElementById(elementId);
  const canvas = await html2canvas(element);
  const pdf = new jsPDF('p', 'mm', 'a4');
  pdf.addImage(canvas, 'PNG', 10, 10, 190, 0);
  pdf.save(filename);
}
```

### 5.2 Server-side PDF (Cloud Function + Puppeteer)
```javascript
// Том/нарийн PDF-д — Cloud Function
exports.generatePDF = functions.https.onCall(async (data) => {
  const { templateId, businessId, referenceId } = data;
  
  // 1. Template + data татах
  const templateData = await getTemplateData(templateId, businessId, referenceId);
  
  // 2. HTML render
  const html = renderTemplate(templateId, templateData);
  
  // 3. Puppeteer → PDF
  const browser = await puppeteer.launch();
  const page = await browser.newPage();
  await page.setContent(html);
  const pdfBuffer = await page.pdf({ format: 'A4', printBackground: true });
  
  // 4. Storage-д хадгалах
  const pdfUrl = await uploadToStorage(pdfBuffer, `pdfs/${businessId}/${filename}.pdf`);
  
  return { url: pdfUrl };
});
```

---

## 6. ЭРХҮҮД

| Permission ID | Нэр |
|---------------|------|
| `pdf.generate_invoice` | Нэхэмжлэл үүсгэх |
| `pdf.generate_receipt` | Баримт үүсгэх |
| `pdf.print` | Хэвлэх |
| `pdf.generate_reports` | Тайлан PDF |

---

*12 template, 2 хэвлэх арга (client/server), thermal дэмжлэг.*
